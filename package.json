{
  "name": "sandra-devconsole",
  "version": "1.0.0",
  "description": "Sandra DevConsole - AI Assistant with MCP Architecture",
  "main": "main.js",
  "author": "Clayton CEO",
  "license": "MIT",
  "scripts": {
    "start": "node main.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "build": "node scripts/build-automated.js",
    "build:dev": "set NODE_ENV=development&& node scripts/build-automated.js",
    "build:prod": "set NODE_ENV=production&& node scripts/build-automated.js",
    "security-audit": "npm audit --audit-level=moderate",
    "security-check": "node scripts/security-check.js",
    "validate:deploy": "node scripts/pre-deploy-validation.js",
    "validate:config": "node scripts/validate-configuration.js",
    "test:smoke": "node tests/smoke-tests.js",
    "test:e2e": "node tests/e2e-tests.js",
    "deploy": "npm run build:prod && npm run validate:deploy && npm run test:smoke && git push origin main && node scripts/verificar-deploy-netlify.js",
    "deploy:staging": "npm run build:prod && npm run validate:deploy && npm run test:smoke && git push origin staging && node scripts/verificar-deploy-netlify.js",
    "deploy:check": "npm run build:prod && npm run validate:deploy && npm run test:smoke",
    "deploy:verify": "node scripts/verificar-deploy-netlify.js",
    "deploy:force-api": "npm run build:prod && npm run validate:deploy && npm run test:smoke && npx netlify deploy --prod"
  },
  "dependencies": {
    "@sentry/node": "^8.55.0",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "electron": "^39.0.0",
    "express": "^4.21.1",
    "helmet": "^8.1.0",
    "marked": "^16.4.1",
    "pdf-parse": "^2.4.5",
    "prom-client": "^15.1.0",
    "sharp": "^0.34.4",
    "twilio": "^5.10.4",
    "uuid": "^11.0.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "electron-builder": "^24.9.1",
    "electron-updater": "^6.1.7",
    "eslint": "^9.38.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
